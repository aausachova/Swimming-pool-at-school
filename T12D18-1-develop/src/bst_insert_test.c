#include <stdio.h>

#include "bst.h"

int main() {
    // struct node* root = NULL;
    // bstree_insert(&root, 5);
    // bstree_insert(&root, 3);

    // Print values
    printf("5\n");
    printf("3");

    return 0;
}